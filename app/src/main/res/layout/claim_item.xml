<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="claim"
            type="com.example.madproject.db.entities.Claim" />
        <variable
            name="viewModel"
            type="com.example.madproject.ClaimsViewModel" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="8dp">

            <TextView
                android:id="@+id/claim_description_text_view"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="@{claim.description}"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <TextView
                android:id="@+id/claim_amount_text_view"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text='@{"Amount: " + String.valueOf(claim.amount)}'
                app:layout_constraintTop_toBottomOf="@+id/claim_description_text_view"
                app:layout_constraintStart_toStartOf="parent" />

            <EditText
                android:id="@+id/edit_amount_edit_text"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:visibility="gone"
                android:inputType="numberDecimal"
                app:layout_constraintTop_toBottomOf="@+id/claim_amount_text_view"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/update_claim_button" />

            <Button
                android:id="@+id/edit_claim_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Edit"
                app:layout_constraintTop_toBottomOf="@+id/edit_amount_edit_text"
                app:layout_constraintEnd_toStartOf="@+id/update_claim_button" />

            <Button
                android:id="@+id/update_claim_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Update"
                android:visibility="gone"
                app:layout_constraintTop_toBottomOf="@+id/edit_amount_edit_text"
                app:layout_constraintEnd_toEndOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>
